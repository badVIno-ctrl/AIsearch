# --- Зависимости Vision Search ---
# Лаунчер (run.sh / run.bat) ставит их сам в локальное окружение,
# так что вручную pip обычно запускать не нужно.

flask>=3.0
pillow>=10.0
numpy>=1.24

# Нейросеть CLIP (текст + картинки). Подтянет torch автоматически.
sentence-transformers>=2.7.0

# --- Необязательно (ускорение и бонусы) ---
# Быстрый поиск на больших коллекциях (если не станется — работаем на numpy):
# faiss-cpu>=1.7
# OCR (поиск по тексту внутри картинок). Также нужен системный tesseract-ocr:
# pytesseract>=0.3.10
